<%= form_for(@agreement) do |f| %>
  <% if @agreement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agreement.errors.count, "error") %> prohibited this agreement from being saved:</h2>

      <ul>
      <% @agreement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :ipp_agreement_guid %><br />
    <%= f.text_field :ipp_agreement_guid %>
  </div>
  <div class="field">
    <%= f.label :contract_manager %><br />
    <%= f.text_field :contract_manager %>
  </div>
  <div class="field">
    <%= f.label :set_up_complete %><br />
    <%= f.check_box :set_up_complete %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
